---
description: Run command with zsh aliases and functions loaded
---

Run the following bash command with the user's shell environment:

```bash
source ~/.zshrc && {{command}}
```

Note: The {{command}} placeholder will be replaced with the user's argument.
